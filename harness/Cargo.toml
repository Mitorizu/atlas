[package]
name = "atlas-ambiguity-oracle"
version = "0.0.0"
edition = "2021"
publish = false

# Dev-time validation only (DESIGN.md §8). This is the single component of the project
# that compiles and runs Rust; it never runs against a user's repository.
[dependencies]
bevy_app = "0.19"
bevy_ecs = { version = "0.19", features = ["debug"] }
serde_json = "1"

[workspace]
